# frozen_string_literal: true

RSpec.describe Weston2022 do
  it "has a version number" do
    expect(Weston2022::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
